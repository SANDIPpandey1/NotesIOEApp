import 'package:flutter/material.dart';

class IOE_Questions extends StatelessWidget {
  const IOE_Questions({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(
          'IOE Questions Page',
          style: TextStyle(
            fontSize: 18,
            fontWeight: FontWeight.bold,
          ),
        ),
      ),
    );
  }
}
